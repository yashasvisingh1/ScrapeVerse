export function EmptyState({ refreshing }: { refreshing: boolean }) { return <div className="empty-state"><span className="empty-icon" aria-hidden="true">◌</span><h3>{refreshing ? 'Fetching the latest products...' : 'No products found for this search.'}</h3><p>{refreshing ? 'Your comparison will appear here shortly.' : 'Try another supported combination from the filters above.'}</p></div> }
